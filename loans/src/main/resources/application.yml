server:
  port: 9000
spring:
  profiles:
    active: "prod"
  application:
    name: "loans"
  datasource:
    url: jdbc:mysql://localhost:3309/loansdb
#    url: jdbc:h2:mem:testdb
#    driverClassName: org.h2.Driver
    username: root
    password: root
#  h2:
#    console:
#      enabled: true
  jpa:
#    database-platform: org.hibernate.dialect.H2Dialect
#    hibernate:
#      ddl-auto: update
    sql:
      init:
        mode: "always"
    show-sql: true
  config:
    import: "optional:configserver:http://localhost:8071"
#  rabbitmq:
#    host: localhost
#    port: 5672
#    username: guest
#    password: guest
logging:
  level:
    com.yossavorn.loans: DEBUG
management:
  endpoints:
    web:
      exposure:
        include: "*"
